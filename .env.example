# QuatomPitch 环境变量样例
# 复制为 .env 后填写。未填的项对应模块会自动跳过，不影响主报告。

# SEC EDGAR 要求所有请求带 User-Agent（格式：应用名 邮箱）。必填，否则 SEC 会拒绝请求。
SEC_USER_AGENT="QuatomPitch admin@example.com"

# FRED 宏观数据（可选）。免费注册获取：https://fred.stlouisfed.org/docs/api/api_key.html
# 留空则跳过宏观模块。
FRED_API_KEY=""

# 数据库文件路径（可选，默认 data/quatompitch.db）
# QUATOMPITCH_DB_PATH="data/quatompitch.db"

# 报告输出目录（可选，默认 reports/）
# QUATOMPITCH_REPORT_DIR="reports"
